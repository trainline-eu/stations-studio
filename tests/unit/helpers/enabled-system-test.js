import { enabledSystem } from '../../../helpers/enabled-system';
import { module, test } from 'qunit';

module('Unit | Helper | enabled system');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = enabledSystem(42);
  assert.ok(result);
});
